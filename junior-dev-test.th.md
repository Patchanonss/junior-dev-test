# แบบทดสอบสำหรับตำแหน่ง Junior Developer (Take-Home)

ใช้เครื่องมือ AI (Copilot, ChatGPT, Claude หรืออื่นๆ) ได้เต็มที่ในทุกส่วนของแบบทดสอบนี้ เราไม่ได้วัดว่าคุณเขียนโค้ดได้หรือไม่ แต่วัดว่าคุณตัดสินใจเชิงออกแบบและอธิบายเหตุผลได้หรือไม่ ถ้าคัดลอกเอกสารนี้ไปวางใน AI ทั้งหมดแล้วส่งคำตอบที่ได้กลับมาเลย คำตอบจะดูตื้นและทั่วไปจนเราสังเกตได้ทันที และคุณจะไม่มีอะไรพูดตอนคุยรอบถัดไป

เวลาโดยประมาณ: 60-90 นาที รวมทั้ง 3 ส่วน ไม่ต้องขัดเกลาให้สมบูรณ์แบบ — คำตอบที่หยาบแต่มีเหตุผลดีย่อมดีกว่าคำตอบที่สวยแต่ทั่วไป

---

## พื้นฐานที่ต้องอ่านก่อน

นี่คือสถาปัตยกรรมจริงฉบับย่อ — พอให้คิดงานนี้ได้ ไม่ใช่ทั้งระบบ

แอปนี้ดูแลสองสภาที่แยกจากกันอิสระ (สภาผู้แทนราษฎร และ วุฒิสภา) ในเวลาใดเวลาหนึ่ง **มีได้อย่างมาก 1 session ต่อสภาที่ถือว่า "live"** เรียกสิ่งนี้ว่า `theOne` ซึ่งควบคุมว่าจอถ่ายทอดสดจะแสดงอะไร

กฎที่โค้ดจริงบังคับใช้อย่างเคร่งครัด:

1. **การตั้งค่า `theOne` ต้องเป็นลำดับนี้เสมอ: เคลียร์ flag ทั้งหมด → ตั้งค่าตัวใหม่ → อัปเดต cache ในหน่วยความจำ → ยิง socket event** ห้ามตั้งค่า `theOne` ใหม่โดยไม่เคลียร์ตัวเก่าก่อน เพราะช่วงสั้นๆ นั้นจะมี 2 sessions ที่อ้างว่า live พร้อมกัน ทำให้จอถ่ายทอดสด race กันว่าจะโชว์อันไหน
2. **สถานะ `theOne` ถูกเก็บ 2 ที่**: ในหน่วยความจำ (อ่านเร็ว) และเป็น flag ในฐานข้อมูล (คงทน) เมื่อ server restart จะอ่าน flag จาก DB มาสร้างสถานะในหน่วยความจำใหม่
3. **ตัวจับเวลาเขียนเวลาที่ใช้ไปลง DB ทุกวินาที (fire-and-forget ไม่มี `await`)** ตั้งใจให้เป็นแบบนี้ เพราะเวอร์ชันก่อนหน้าเขียนถี่น้อยกว่านี้ แล้ว server ล่มระหว่างประชุมทำให้เวลาที่ใช้ไปหายไปเยอะ ความเสียหายที่ยอมรับได้สูงสุดคือ 1 วินาที
4. แต่ละสภามี live session ที่ติดตามแยกจากกันโดยสมบูรณ์ — ไม่มีอะไรของ `theOne` ฝั่งสภาผู้แทนฯ ควรไปแตะฝั่งวุฒิสภา และในทางกลับกันก็เช่นกัน

---

## ส่วนที่ A — เอกสารออกแบบ (ไม่ต้องเขียนโค้ด)

**Feature ที่ขอมา:** บางครั้งรัฐสภาจัด "การประชุมร่วมกัน" ที่ทั้งสองสภาประชุมพร้อมกันและแสดงบนจอถ่ายทอดสดเดียวกันด้วยตัวจับเวลาเดียว ควบคุมโดยประธานของสภาที่กำลังทำหน้าที่ประธานในขณะนั้น เมื่อการประชุมร่วมสิ้นสุด แต่ละสภาต้องกลับไปทำงานต่อจากจุดที่ session อิสระของตัวเองค้างไว้พอดี — เหมือนถูก "พัก" ไว้ระหว่างประชุมร่วม ไม่ใช่ถูกแทนที่

ข้อจำกัด:
- ห้ามสร้าง collection ใหม่ในฐานข้อมูล
- พฤติกรรมเดิมของแต่ละสภาแบบเดี่ยว (ไม่มีประชุมร่วม) ต้องทำงานเหมือนเดิมทุกประการ
- ต้องรอดจาก server restart *ระหว่าง* ที่มีการประชุมร่วมอยู่ โดยไม่สูญเสียข้อมูลว่าสภาไหนถูกพักไว้ หรือแต่ละฝ่ายใช้เวลาไปเท่าไหร่แล้ว

เขียนประมาณครึ่งหน้า ใช้ bullet points ได้ ครอบคลุม:

1. เพิ่ม state อะไร เก็บไว้ที่ไหน (memory หรือ DB) — 1-2 ประโยคพอ
2. ลำดับเหตุการณ์ตอนเริ่ม/จบการประชุมร่วม — แค่พอให้เห็นว่าอะไรถูกเคลียร์ ตั้งค่า คืนค่า
3. race condition หรือ edge case ช่วง restart อย่างน้อย 1 อย่างที่คุณคิดถึง และวิธีที่ออกแบบเพื่อหลีกเลี่ยง
4. สิ่งหนึ่งที่คุณตัดสินใจ *ไม่ทำ* โดยเจตนา พร้อมเหตุผล

เราให้น้ำหนักกับข้อ 3 และ 4 มากกว่าข้อ 1-2 มาก ใครๆ ก็อธิบาย happy path ได้

---

## ส่วนที่ B — รีวิว PR นี้

เพื่อนร่วมทีมเปิด PR ด้านล่างนี้กับโค้ด sandbox ที่ `joint-sitting/theOneStore.js` ดูที่ `joint-sitting/flawed-pr.diff`

คำอธิบาย PR เขียนว่า: *"Cleans up theOneStore — batches the per-tick DB write to reduce load, and simplifies setTheOne to one less branch."*

เขียนรีวิวสั้นๆ (bullet points ได้):

- คุณจะ approve PR นี้ไหม ถ้าไม่ ทำไม — ระบุให้ชัดว่าอะไรจะพังและพังภายใต้เงื่อนไขไหน (อย่าตอบแค่ว่า "ผิดกฎข้อ 3")
- มีส่วนไหนใน PR นี้ที่เป็นการปรับปรุงที่ดีจริง ๆ และควรเก็บไว้ แยกออกจากส่วนที่มีปัญหาไหม

---

## ส่วนที่ C — POC เล็กๆ

ใน `joint-sitting/` มีเวอร์ชันย่อแบบ standalone ของ pattern `theOneStore` จริง สำหรับสองสภาที่แยกจากกัน (`representative` และ `senate`) — ไม่มี database ไม่มี socket มีแค่ state ในหน่วยความจำและ fake persistence layer เพื่อให้รันและเทสได้โดยไม่ต้อง setup แอปทั้งระบบ

ให้ implement `startJointSitting(presidingChamber)` และ `endJointSitting()` ตามการออกแบบในส่วน A ของคุณ โดยใช้ interface ที่มีอยู่แล้วใน `theOneStore.js` แล้วเพิ่ม test อีก 1 เคสสำหรับ edge case จากส่วน A ข้อ 3

รันด้วยคำสั่ง `node joint-sitting/theOneStore.test.js` — ควร exit 0

เราไม่ได้ดูความสวยงามของโค้ดตรงนี้ แต่ดูว่า POC ตรงกับดีไซน์ที่คุณเขียนไว้ในส่วน A จริงไหม หรือสองอย่างนี้ไปคนละทางกันตอนที่ต้องเขียนจริง

---

## การส่งงาน

ส่งกลับมา: เอกสารส่วน A, รีวิวส่วน B, และ `theOneStore.js` + `theOneStore.test.js` ที่แก้แล้ว ระบุด้วยว่าใช้ AI ช่วยตรงไหนบ้าง (ไม่ใช่เรื่องลบ แค่อยากรู้ว่าส่วนไหนเป็นของคุณเอง)

เราจะนัดคุยตามหลัง 20-30 นาที เพื่อให้คุณอธิบายการตัดสินใจของคุณ
