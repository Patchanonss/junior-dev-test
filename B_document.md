# การ Review โค้ด PR (flawed-pr.diff)

**สถานะ PR:** ❌ **Reject**

จากการตรวจสอบโค้ดที่ถูกส่งเข้ามา ไม่สามารถอนุมัติได้เนื่องจากละเมิด Business Requirement ที่สำคัญ 2 จุด และก่อให้เกิดบั๊กร้ายแรง ดังนี้:

### 1. การสลับลำดับการทำงานใน `setTheOne` ก่อให้เกิด Data Inconsistency ร้ายแรง
**โค้ดที่ถูกแก้:** มีการเรียก `emit` ส่ง socket event ไปหา client ก่อนที่จะอัปเดตลง Database (`fakeDb`)
- **เหตุผลที่ปฏิเสธ:** ถ้า Server พัง (Crash) หลังจากบรรทัดที่เรา `emit` socket event ออกไปแล้ว มันจะเกิด Data Inconsistency ทันที เพราะฝั่งจอถ่ายทอดสดจะแสดงผลว่า session ใหม่กำลัง live อยู่ แต่ในมุมของ Server ข้อมูลใน memory ที่อัปเดตไปแล้วพอเซิฟล่มมันก็หายไปหมด พอจะไปดึง state จาก DB กลับมาตอน restart ก็ไม่มีข้อมูลอีก เพราะทุกอย่างมันล่มก่อนที่จะถูกเขียนลง DB ทำให้ระบบพัง ข้อมูลขัดแย้งกันอย่างสมบูรณ์
- **วิธีแก้:** ต้องกลับไปใช้ลำดับเดิมตามกฎของระบบ (เขียนลง DB ให้เสร็จก่อน -> อัปเดต Memory -> แล้วค่อยยิง Socket Event)

### 2. การทำ Batch DB Writes ในฟังก์ชัน `tick` ละเมิดเงื่อนไขความเสียหายที่ยอมรับได้
**โค้ดที่ถูกแก้:** มีการเก็บค่าไว้ใน `pendingWrite` และจะยิงลง DB ทุกๆ 10 วินาที (`secondsElapsed % 10 === 0`) แทนการเขียนทุกวินาที
- **เหตุผลที่ปฏิเสธ:** โจทย์บอกเองว่ายอมรับความเสียหายได้มากสุดคือ 1 วินาที ทำให้การ batch write น้ันเกิดขึ้นไม่ได้ เพราะความเสียหายมันเกิดขึ้นถ้าเกิดมันเกิน 1 วินาทีแน่นอน ดังนั้นการที่เราดองข้อมูลไว้ 10 วินาที ถ้าเซิร์ฟเวอร์ล่มในวินาทีที่ 9 ข้อมูลเวลาอภิปรายจะสูญหายถึง 9 วินาที ซึ่งเกินขอบเขตที่ธุรกิจรับได้
- **วิธีแก้:** ต้องรักษาโค้ดเดิมไว้ คือยิงลง DB แบบ fire-and-forget ทุกๆ 1 วินาทีตาม Requirement ดั้งเดิม

---

### บทสรุป: มีส่วนไหนใน PR นี้ที่เป็นการปรับปรุงที่ดีและควรเก็บไว้หรือไม่?
**ตอบ:** **ไม่มีโค้ดส่วนไหนที่ควรเก็บไว้เลย** 
แม้ว่าความตั้งใจของผู้เขียน PR ที่ต้องการลดภาระ (Load) ของ Database ด้วยการทำ Batch Write จะเป็นแนวคิดที่ดีในเชิงสถาปัตยกรรมระบบทั่วๆ ไป แต่วิธีการที่นำมาใช้นั้นผิดบริบทอย่างสิ้นเชิง โค้ดทั้งหมดที่ถูกแก้ไขไปขัดแย้งกับ Business Requirement โดยตรง (ยอมรับความเสียหายได้แค่ 1 วินาที) และยังละเมิดกฎเรื่อง Data Integrity (การเซฟลง DB ก่อน Emit Event) ดังนั้นจึงต้อง Reject โค้ดชุดนี้ทั้งหมด
